// Genere par scripts/generate-gone-images.mjs — npm run images:gone
// Vide tant que le nettoyage des images orphelines n'a pas ete lance.

export const goneImageStems: ReadonlySet<string> = new Set([]);
